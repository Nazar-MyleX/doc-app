import styled from '@emotion/styled';

export const RoundIcon = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1.7px solid #338EB4;
  border-radius: 100%;
  width: ${props => props.width};
  height: ${props => props.height};
`
